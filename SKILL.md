---
name: minquzi
description: 解析微信聊天记录截图，提取含【离】字或【消费】字的业绩数据，生成 Excel 表格。使用 Claude 视觉能力识别图片文字，按规则提取尾号、业绩等字段。用于微信群组业绩统计场景。
---

# minquzi — 微信聊天记录业绩提取 Skill

本 Skill 用于从微信群聊截图中识别含【离】字或【消费】字的业绩记录，按规则提取字段并输出为 Excel 表格。

---

## 一、可配置项（随时修改）

### 1.1 特殊群组倍率表 `config.yaml`

某些群组的**实际业绩**需要在默认规则基础上**再乘以一个倍率**。

```yaml
special_groups:
  - name: "郑州管城51部"
    multiplier: 10
  # 添加新群组示例：
  # - name: "某某群组"
  #   multiplier: 10
```

匹配方式：**群组名称包含**该关键字即命中（模糊匹配）。

### 1.2 群组点位映射表 `config.yaml`

每个群组的点位由用户手动配置：

```yaml
group_points:
  "天津zz26南开和平门店（五日）": 0.3
  "广深杭果兮": 0.3
  "郑州管城51部": 0.25
  # 添加新群组示例：
  # "某某群组": 0.3
```

匹配方式：**群组名称精确匹配**（包含括号和特殊符号）。

如果群组未配置，点位留空，应结也留空。

---

## 二、Excel 字段定义（共 11 列）

| 列 | 说明 | 数据来源 |
|---|---|---|
| 日期 | 格式 `4月30日` | 调用时手动传入 |
| 群组 | 图片顶部群名称，去掉末尾 `(xxx)` 数字 | 图片识别 |
| 尾号 | 手机号后4位 | 图片识别（按规则提取） |
| 原始业绩 | 图片中的原始数字（可能带小数） | 图片识别（按规则提取） |
| 实际业绩 | 换算后的整数 | 计算 |
| 点位 | 群组对应的点位 | `config.yaml` 映射表 |
| 应结 | 实际业绩 × 点位 | 计算 |
| 总监 | 总监名称 | 手动录入（留空） |
| 奖励 | 奖励金额 | 手动录入（留空） |
| AI识别异常 | `是` / `否` | 自动判断 |
| 原始数据 | 图片中所有识别到的文字 | 图片识别（全部文字） |

---

## 三、识别与提取规则

### 3.1 群组提取

- 取图片**顶部第一行**文字
- 去掉末尾的 `(数字)`
- 示例：`天津南开和平门店(126)` → `天津南开和平门店`

### 3.2 样本筛选

只处理**符合以下任一条件**的消息段（以下称"样本"）：

1. 含【离】字（包括"离"、"离店"、"已离店"等）
2. 不含【离】字但含【消费】字（考虑 OCR 识别错误导致"离"被误识别）

### 3.3 尾号提取

在样本中按以下优先级提取**4位连续数字**作为尾号：

1. `尾号：XXXX` 格式（标签明确）
2. 段落开头的4位数字（如 `6514消120...` → `6514`）
3. 字符串中第一个连续4位数字

如果样本中没有4位数字 → 尾号留空。

### 3.4 业绩提取（优先级从高到低）

**规则 A：含【离】字时**

- 取【离】字**前面最近的数字**作为原始业绩
- 特殊情况：若【离】字**前无数字**但**后面**有【消费】，走规则 B
- 特殊情况：若【离】字前只有整数（非金额）需上下文判断

**规则 B：含【消费】字时**

- 取【消费】后的数字作为原始业绩
- 若【消费】后还有【余】或【剩】字，则**取【余】/【剩】后的数字**（表示最终结余才是业绩）

**规则 C：都不满足**

- 原始业绩、实际业绩、尾号字段留空
- `AI识别异常` 字段 = `是`
- 原始数据字段照常录入整段文字，用户人工处理

### 3.5 实际业绩换算

```
默认换算：
  - 原始业绩带小数点（如 400.0）→ 实际业绩 = 400.0 × 10 = 4000
  - 原始业绩不带小数点（如 120） → 实际业绩 = 120

特殊群组额外倍率：
  - 若群组名包含 special_groups 中的关键字
  - 实际业绩 = 默认换算结果 × multiplier
  - 例：郑州管城51部 "0048总120离" → 120 × 10 = 1200
  - 例：郑州管城51部 "xxx消400.0余0离" → 400.0 × 10 × 10 = 40000
```

### 3.6 应结计算

```
应结 = 实际业绩 × 点位
```

若点位未配置，应结留空。

### 3.7 AI识别异常判断

以下情况 `AI识别异常 = 是`，其他 = `否`：

- 含【离】字但无法提取到业绩数字
- 业绩数字格式明显异常
- 尾号无法识别

---

## 四、使用流程

### 4.1 目录结构

```
minquzi/
├── SKILL.md              # 本文档
├── config.yaml           # 配置表（倍率、点位映射）
├── parse_wechat.py       # 主脚本：解析 + 生成 Excel
├── requirements.txt      # Python 依赖
├── vendor/               # 存放微信截图
│   └── *.png
└── output/               # 生成的 Excel 输出目录
```

### 4.2 触发方式

当用户请求包含以下意图时触发本 Skill：

- "解析微信聊天记录截图"
- "从微信截图生成业绩 Excel"
- "识别 vendor 目录下的图片"
- "minquzi" / "民取字"

### 4.3 执行步骤（AI 代理遵循）

**重要：本 Skill 采用 "AI 识别 → JSON → Python 换算" 两段式流程，AI 不负责换算和写 Excel。**

1. **读取配置**：读取 `config.yaml`，记下 `special_groups`（用于判断是否需要再乘倍率，本阶段只需识别不需换算）
2. **获取日期**：向用户询问日期（格式 `4月30日`）。如果用户一次提交多天，支持按图片组分别指定
3. **读取图片**：遍历 `minquzi/vendor/` 下所有图片，使用 `read` 工具（Claude 视觉能力）逐张识别
4. **逐张识别**：对每张图片
   - 提取顶部群组名称（去括号）
   - 提取全部文字作为 `raw_text`（保留换行）
   - 遍历每个样本（含【离】或【消费】的消息段）
   - 按 3.3 规则提取尾号
   - 按 3.4 规则提取**原始业绩字符串**（保留小数点，如 `"450.0"` 或 `"120"`）
   - 按 3.7 规则判断 AI识别异常
5. **组装 JSON**：按 4.5 Schema 把所有样本组装成 JSON 数组，写入 `minquzi/data_{日期}.json`
6. **展示预览**：以 Markdown 表格形式展示给用户校验（含推算后的实际业绩便于核对）
7. **生成 Excel**：用户确认后，执行命令：
   ```bash
   python3 parse_wechat.py --from-json data_{日期}.json --date "{日期}" --output output/
   ```
   脚本会自动应用 `config.yaml` 的倍率和点位，计算实际业绩和应结，并生成 `.xlsx`

### 4.4 命令行用法

```bash
# 推荐：AI 识别后生成 JSON，然后调用脚本
python3 parse_wechat.py --from-json data_4月30日.json --date "4月30日" --output output/
```

输出文件：`output/业绩汇总_{日期}_{时间戳}.xlsx`

### 4.5 JSON Schema（AI 识别结果的中转格式）

每张图片的每条符合条件的样本组装为一个对象，所有样本放入一个 JSON 数组：

```json
[
  {
    "date": "4月30日",
    "group": "广深杭果兮",
    "tail": "6514",
    "raw_perf": "450.0",
    "director": "",
    "reward": "",
    "ai_abnormal": false,
    "raw_text": "广深杭果兮(438)\nG\n6917服务中\n...\n6514消120.0+...余450.0离"
  }
]
```

字段规则：

| 字段 | 类型 | 说明 |
|---|---|---|
| `date` | string | 用户提供的日期，如 `"4月30日"` |
| `group` | string | 群组名（去括号数字后） |
| `tail` | string | 4位尾号；无法识别留空 `""` |
| `raw_perf` | string | 原始业绩**字符串**，保留小数点（如 `"450.0"`、`"120"`）；无法识别留空 `""` |
| `director` | string | 总监（本阶段留空） |
| `reward` | string | 奖励（本阶段留空） |
| `ai_abnormal` | bool | 识别异常标记：无法提取业绩时填 `true` |
| `raw_text` | string | 图片中**所有**识别到的文字，保留换行 `\n` |

**注意**：
- 同一张图有多条符合条件的样本时，生成多条 JSON 对象，`raw_text` 都填**整张图的全部文字**（相同值）
- `raw_perf` 必须是**字符串**，不要写成数字（保留小数点判断换算规则）
- 不要在 JSON 里做换算，Python 脚本会根据 `raw_perf` + `config.yaml` 自动计算 `实际业绩` 和 `应结`

---

## 五、关键注意点

1. **图片识别依赖 Claude 视觉能力**，不使用本地 OCR 库（本地 OCR 对微信字体和 emoji 识别率差）
2. **日期字段不从图片识别**，由用户在调用时传入
3. **总监、奖励**字段本 Skill 不填充，用户手动填写
4. **特殊群组倍率可叠加**：默认换算规则（小数×10）之后再应用特殊群组倍率
5. **一次可处理多张图片**，每张图片的每条样本独立成一行
6. **识别异常的行**必须录入表格，由用户人工核查，不能静默丢弃

---

## 六、样例参考（已校验的识别结果）

| 群组 | 原始消息 | 尾号 | 原始业绩 | 实际业绩 |
|---|---|---|---|---|
| 广深杭果兮 | `6514消120.0+180.0+200.0=500.0扣50.0余450.0离` | 6514 | 450.0 | 4500 |
| 长沙岳麓（日） | `2036肖80.0退120.0离不退就` | 2036 | 120.0 | 1200 |
| 金牛九里提DK134群 | `0302消费500.0已离店` | 0302 | 500.0 | 5000 |
| 江浙沪 全位置 | `7925前120.0退90.0余30.0离` | 7925 | 30.0 | 300 |
| 郑州管城51部 ⭐ | `0048总120离` | 0048 | 120 | 1200（×10） |
| 百子湾门店888组 | `1366已离店消费100.0` | 1366 | 100.0 | 1000 |
| 紫 闵行 平安鸟36队 | `@花开富贵 新客一位1674开卡700已离店` | 1674 | 700 | 700 |
| 南宁兴宁区 村长 | `2615消1000.0扣查100.0余900.0离` | 2615 | 900.0 | 9000 |
| 朝阳周庄s2部2-12 | `6981v160.0+V340.0合500.0扣100.0余400.0米t，离` | 6981 | 400.0 | 4000 |
| 广深杭果兮 | `6052海选挑离` | 6052 | （空，AI识别异常=是） | — |
